server:
  port: 8080

spring:
  cloud:
    stream:
      bindings:
        worksChannel:
          binder: rabbit
          destination: work.exchange
          contentType: application/json
          #group: testgroup, testgroup1
  rabbitmq:
      password: "rabbitmq"
      username: "rabbitmq"